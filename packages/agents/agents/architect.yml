name: architect
description: "Strategic planner. Handles triage (replaces refiner), designs architecture, writes spec documents, decomposes work into atomic tasks with execution strategy. Spawns spec-reviewer subagents. Never writes code."
model: opus
tools:
  - Read
  - Glob
  - Grep
  - WebSearch
  - WebFetch
  - Agent
sandbox: readonly
maxTurns: 75
prompt: ../prompts/architect.md
